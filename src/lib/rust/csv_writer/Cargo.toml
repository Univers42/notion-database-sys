[package]
name = "csv_writer"
version = "0.1.0"
edition = "2021"
description = "CLI utility to read/update/write CSV seed files atomically"

[dependencies]
csv = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
clap = { version = "4", features = ["derive"] }
